<Window x:Class="PL.ProductListWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:Enum="clr-namespace:BO;assembly=BL"
        xmlns:sys="clr-namespace:System;assembly=mscorlib"
        xmlns:local="clr-namespace:PL"
        DataContext="{Binding Path=Prods, RelativeSource={RelativeSource self}}"
        mc:Ignorable="d"
        Title="ShowProduct" Height="500" Width="800">
    <Window.Resources>
        <ObjectDataProvider MethodName="GetValues"
                    ObjectType="{x:Type sys:Enum}"
                    x:Key="allCategories">
            <ObjectDataProvider.MethodParameters>
                <x:Type TypeName="Enum:Categories" />
            </ObjectDataProvider.MethodParameters>
        </ObjectDataProvider>
        <DataTemplate x:Key="MyDataTemplate" DataType="local:ProductForList">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                </Grid.RowDefinitions>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="*" />
                </Grid.ColumnDefinitions>
                <TextBlock Grid.Column="0" Text="{Binding Path=ID}" />
                <TextBlock Grid.Column="1" Text="{Binding Path=Name}" />
                <TextBlock Grid.Column="2" Text="{Binding Path=Price}" />
                <TextBlock Grid.Column="3" Text="{Binding Path=Category}" />
            </Grid>
        </DataTemplate>
        <Style TargetType="Label">
            <Setter Property="FontFamily" Value="Dubai Medium" />
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="HorizontalContentAlignment" Value="Left"/>
            <Setter Property="Height" Value="30"/>
        </Style>
    </Window.Resources>
    <Grid Margin="0,0,0,0">
        <Grid.RowDefinitions>
            <RowDefinition Height="*" />
            <RowDefinition Height="*" />
            <RowDefinition Height="11*" />
            <RowDefinition Height="2*" />
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition  Width="*"/>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition  Width="*"/>
            <ColumnDefinition  Width="*"/>
            <ColumnDefinition Width="20"/>
            <ColumnDefinition Width="20"/>
            <ColumnDefinition Width="20"/>
            <ColumnDefinition  Width="80"/>

        </Grid.ColumnDefinitions>
        <Label Content="Id Product" Grid.Row="1" Grid.Column="0"/>
        <Label Content="Product Name" Grid.Row="1" Grid.Column="1"/>
        <Label Content="Price" Grid.Row="1" Grid.Column="2"/>
        <Label Content="Category" Grid.Row="1" Grid.Column="3"/>
        <Label Content="Is In Stock" Grid.Row="1" Grid.Column="4"/>
        <Label Content="Amont In Stock" Grid.Row="1" Grid.Column="5"/>
        <ComboBox x:Name="CategoriesSelector" Grid.Row="0" Grid.ColumnSpan="5"  SelectionChanged="CategoriesSelector_SelectionChanged" Width="132" Height="38" SelectedItem="Choose_Category"/>
        <ListView ItemsSource="{Binding}" Background="#FFF7E4C6" Grid.Row="2" Grid.ColumnSpan="10"  x:Name="ProductListview"  ItemTemplate="{StaticResource MyDataTemplate}"
HorizontalContentAlignment="Stretch"  MouseDoubleClick="ProductListview_MouseDoubleClick"/>
        <Button Content="Add new Product" Grid.Row="3" Grid.ColumnSpan="10" 
Click="Button_Click" Width="135" Height="46" Margin="0,10,0,0" FontSize="15" />
    </Grid>
</Window>