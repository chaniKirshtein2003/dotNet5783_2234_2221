<Window x:Class="PL.ProductListWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:Enum="clr-namespace:BO;assembly=BL"
        xmlns:sys="clr-namespace:System;assembly=mscorlib"
        xmlns:local="clr-namespace:PL"
        DataContext="{Binding Path=prods, RelativeSource={RelativeSource self}}"
        mc:Ignorable="d"
        Title="ShowProduct" Height="500" Width="800">
    <Window.Resources>
            <!--<ObjectDataProvider MethodName="GetValues"
                    ObjectType="{x:Type sys:Enum}"
                    x:Key="allCategories">
                <ObjectDataProvider.MethodParameters>
                    <x:Type TypeName="Enum:Categories" />
                </ObjectDataProvider.MethodParameters>
            </ObjectDataProvider>-->
            <DataTemplate x:Key="MyDataTemplate" DataType="local:ProductForList">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                </Grid.RowDefinitions>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="*" />
                </Grid.ColumnDefinitions>
                <TextBlock Grid.Column="0" Text="{Binding Path=ID}" />
                <TextBlock Grid.Column="1" Text="{Binding Path=Name}" />
                <TextBlock Grid.Column="2" Text="{Binding Path=Price}" />
                <TextBlock Grid.Column="3" Text="{Binding Path=Category}" />
            </Grid>
        </DataTemplate>
    </Window.Resources>
    <StackPanel>
        <ComboBox x:Name="CategoriesSelector" SelectionChanged="CategoriesSelector_SelectionChanged" Width="132" Height="38" SelectedItem="Choose_Category"/>
        <ListView ItemsSource="{Binding}" x:Name="ProductListview"  ItemTemplate="{StaticResource MyDataTemplate}"
HorizontalContentAlignment="Stretch"  MouseDoubleClick="ProductListview_MouseDoubleClick"/>
        <Button Content="Add new Product"
Click="Button_Click" Width="135" Height="46" Margin="0,10,0,0" FontSize="15" />
    </StackPanel>
</Window>
<!--<Grid Name="MainGrid">
        <Grid Name="UpGrid" HorizontalAlignment="Stretch" Height="auto" Grid.Row="0"
VerticalAlignment="Stretch" Width="auto" RenderTransformOrigin="0.506,0.769">
            <ComboBox Name="CategoriesSelector" HorizontalAlignment= "Stretch" VerticalAlignment="Stretch"
Grid.Column="2" SelectionChanged="CategoriesSelector_SelectionChanged"/>
            <Label Grid.Column="0" Grid.ColumnSpan="2" FontSize="16" >
                <Label Content="Category" Height="33" Width="190"/>
            </Label>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="369*"/>
                <ColumnDefinition Width="31*"/>
                <ColumnDefinition Width="400*" />
            </Grid.ColumnDefinitions >
        </Grid>
        <ListView ItemsSource="{Binding}" Grid.Row="1" x:Name="ProductListview" MouseDoubleClick="ProductListview_MouseDoubleClick" />
        <Button x:Name="AddUpdateBtn" Content="Add" Margin="46,184,608,200" Background="#FFF37E7E" FontSize="14" Height="50"/>
        <Button Content="Add new Product" Grid.Row="2" HorizontalAlignment="Right" Margin="5" Padding="5" Click="Button_Click"/>
        <Grid.RowDefinitions>
            <RowDefinition Height="40*"/>
            <RowDefinition Height="500*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

    </Grid>
</Window>-->